import SigninForm from '@/components/auth/signin-form';

/**
 * Signin page
 */
export default function SigninPage() {
  return <SigninForm />;
}

export const metadata = {
  title: 'Sign In - UI Note Service',
  description: 'Sign in to your UI Note Service account',
};
