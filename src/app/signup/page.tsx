import SignupForm from '@/components/auth/signup-form';

/**
 * Signup page
 */
export default function SignupPage() {
  return <SignupForm />;
}

export const metadata = {
  title: 'Sign Up - UI Note Service',
  description: 'Create a new account for UI Note Service',
};
